import { Component } from '@angular/core';

/** Placeholder invisible pour forcer le remount de la page après un switch de société. */
@Component({
  selector: 'app-route-reload',
  standalone: true,
  template: ''
})
export class RouteReloadComponent {}
