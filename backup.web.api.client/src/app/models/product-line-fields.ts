/** Champs minimaux d'une ligne document pour l'autocomplete produit. */
export interface ProductLineFields {
  productKey: string;
  description: string;
  unitPrice: number;
  vatRate: number;
}
